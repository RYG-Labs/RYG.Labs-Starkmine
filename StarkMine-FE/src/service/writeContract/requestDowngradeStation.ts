import { AccountInterface } from "starknet";

const requestDowngradeStation = (account: AccountInterface , stationId: number, stationLevel: number) => {
  try {
    
  } catch (error) {
    
  }
};

export default requestDowngradeStation;